import { SimplePage } from "@/components/simple-page";

const questions = [["Where can I see my order status?","Sign in and open My orders. Each order shows its current status, payment state, delivery information, and available actions."],["How do I request a return?","Eligible delivered items appear on the Returns page. Submit the item, quantity, reason, requested resolution, and any useful evidence."],["How can I get help with an order?","Open a support ticket from your account or use the support action on an order. Replies remain in your private ticket thread."],["Where can I find delivery and payment terms?","Review the published shipping, payment, cancellation, privacy, and return-policy pages. Pages marked for legal review are not final legal approval."]];
export default function Page() { return <SimplePage title="Frequently asked questions"><p className="text-lg text-navy">Quick guidance for using your KhelaGhor account and order tools.</p><div className="space-y-3">{questions.map(([question,answer], index) => <details key={question} className="group rounded-2xl border border-[var(--border)] bg-white open:shadow-[var(--shadow-sm)]"><summary className="flex min-h-14 cursor-pointer items-center justify-between gap-4 px-5 py-4 font-black text-navy focus-visible:outline-none"><span>{question}</span><span className="text-xl text-coral transition group-open:rotate-45" aria-hidden="true">+</span></summary><div className="border-t border-[var(--border)] px-5 py-4 text-sm leading-7" id={`faq-${index}`}>{answer}</div></details>)}</div></SimplePage>; }
